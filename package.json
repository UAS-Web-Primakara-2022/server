{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "dev": "NODE_ENV=development ts-node-dev --respawn --transpileOnly ./app.ts",
    "clean": "rm -rf dist",
    "compile": "npm run clean && tsc",
    "prod": "tsc && node dist/bin/http.js"
  },
  "author": "",
  "license": "ISC"
}
